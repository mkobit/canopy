{
  "name": "@canopy/schema",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@canopy/types": "workspace:*",
    "zod": "^3.0.0"
  },
  "devDependencies": {
     "typescript": "5.9.3",
    "vitest": "1.6.0"
  }
}
