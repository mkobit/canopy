# Storage file package

This package provides a file-system-based event log store for Canopy.
It implements the `EventLogStore` interface.
It is intended for single-device read and write workflows.
It stores events in JSONL format.
