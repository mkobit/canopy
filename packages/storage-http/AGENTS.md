# @canopy/storage-http

`EventLogStore` (defined in `@canopy/graph`) implemented over HTTP.

## Allowed dependencies

`@canopy/graph` only.
External: none.

## Forbidden

- No React, no UI concerns.
- Do not redefine `EventLogStore` or `EventLogQueryOptions` — import them from `@canopy/graph`.
