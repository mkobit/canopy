{
  "name": "@canopy/storage-http",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "bun test ./src"
  },
  "dependencies": {
    "@canopy/graph": "workspace:*"
  },
  "devDependencies": {
    "temporal-polyfill": "^0.3.2",
    "typescript": "6.0.3"
  }
}
