{
  "name": "@canopy/queries",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "bun test ./src ./tests"
  },
  "dependencies": {
    "@canopy/graph": "workspace:*",
    "remeda": "^2.39.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "temporal-polyfill": "^1.0.1",
    "typescript": "6.0.3"
  }
}
