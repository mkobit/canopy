{
  "name": "@canopy/queries",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "bun test ./src ./tests",
    "bench:query-projection": "bun run scripts/bench-query-projection.ts"
  },
  "dependencies": {
    "@canopy/graph": "workspace:*",
    "remeda": "^2.39.0"
  },
  "devDependencies": {
    "@canopy/storage": "workspace:*",
    "fast-check": "^4.9.0",
    "typescript": "6.0.3"
  }
}
