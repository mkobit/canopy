## 1. Upgrades and compiler validation (canopy-w88.1)

- [ ] 1.1 Update `typescript` version in `package.json` to `7.0.2` and run `bun install`.
- [ ] 1.2 Run compile and typecheck gates, resolving any compile-time errors.
- [ ] 1.3 Update or verify `@typescript-eslint` packages compatibility to ensure clean parsing.

## 2. Test execution and CI validation (canopy-w88.2)

- [ ] 2.1 Run all unit and integration tests under the new TypeScript toolchain.
- [ ] 2.2 Run `eslint` check to ensure zero linting errors.
