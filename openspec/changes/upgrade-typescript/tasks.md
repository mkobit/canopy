## 1. Upgrades and compiler validation (canopy-w88.1)

- [x] 1.1 Update `typescript` version in `package.json` to `7.0.2` and run `bun install`.
- [x] 1.2 Run compile and typecheck gates, resolving any compile-time errors.
- [x] 1.3 Update or verify `@typescript-eslint` packages compatibility to ensure clean parsing.

## 2. Test execution and CI validation (canopy-w88.2)

- [x] 2.1 Run all unit and integration tests under the new TypeScript toolchain.
- [x] 2.2 Run `eslint` check to ensure zero linting errors.
