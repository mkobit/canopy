# Tasks: Storybook setup in `apps/web`

- [ ] 1. Add Storybook dependencies and scripts to `apps/web/package.json` and root `package.json` <!-- id: 1 -->
- [ ] 2. Create Storybook configuration in `apps/web/.storybook/` (`main.ts` and `preview.tsx`) <!-- id: 2 -->
- [ ] 3. Create stories for `CustomNode`, `CustomEdge`, and `GraphCanvas` in `apps/web/src/components/graph/` <!-- id: 3 -->
- [ ] 4. Run Storybook build and verify clean execution (`bun run storybook`) <!-- id: 4 -->
- [ ] 5. Run full quality gates (`bun run build && bun run lint && bun run typecheck && bun test`) <!-- id: 5 -->
